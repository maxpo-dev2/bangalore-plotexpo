query GetExhibitorsByProject($projectId: String!) {
  getExhibitorsByProject(projectId: $projectId) {
    companyName
    createdAt
    description
    id
    badge
    linkedinUrl
    logoUrl
    projectId
    website
  }
}
